{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // required for "paths"
    "paths": {
      "@/*": ["./*"],
      "mobile-payments-sdk-react-native": ["../src/index"] // 👈 add this line
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
